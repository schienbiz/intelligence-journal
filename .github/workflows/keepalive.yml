name: Keep Alive

on:
  schedule:
    - cron: "*/14 * * * *"   # every 14 min — prevents Render free tier spin-down (15-min idle)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sf https://intelligence-journal.onrender.com/health
